
//===================================================================
// 変数を登録する
//===================================================================
@sys:bgm(ＢＧＭ娯楽の島)
@sys:se(ＳＥガヤ)

// 背景を追加したい場合
// @sys:background(変数名)
@sys:background(背景娯楽の島)
@sys:background(背景黒)
@sys:background(背景商店街)
@sys:background(背景噴水広場)
@sys:background(背景遊園地)

// 名前を追加したい場合
// @sys:name(変数名)
@sys:name(名前アレン)
@sys:name(名前クロエ)
@sys:name(名前八百屋)
@sys:name(名前パン屋)
@sys:name(名前ピエロの少年)
@sys:name(名前女性)

// 人物画像を追加したい場合
// @sys:human(変数名)
@sys:human(クロエ怒り)
@sys:human(クロエ普通)
@sys:human(クロエ喜び)
@sys:human(クロエ驚き)
//===================================================================
// 変数を登録する
//===================================================================
